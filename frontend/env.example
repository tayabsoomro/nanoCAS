# Backend URL when the UI is served from a different origin than the API.
# Leave empty to use the CRA dev proxy (package.json "proxy") / same origin.
REACT_APP_API_ENDPOINT=http://localhost:5007
# Force Socket.IO transports (e.g. "polling") on hosts that block WebSockets.
# REACT_APP_SOCKET_TRANSPORTS=
